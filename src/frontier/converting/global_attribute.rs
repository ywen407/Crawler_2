use crate::frontier::coordinator::Coordinator;

impl Coordinator{
     /* implement */
    /*
        각 사이트 URL마다 unique한 웹 사이트 ID를 부여한다.
        key:URL, value(siteID,domainID,clusterID)를 부여해야 한다.
    */
}
