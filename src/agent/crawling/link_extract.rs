use crate::agent::agent::Agent;

impl Agent {
     /* implement */
    /*
      웹 페이지에서 URL들을 추출하여 사이트 내부의 URL이고 이미 수집된 URL일 경우
      Visited URL에 추가하고, 아직 수집되지 않은 경우 ToVisitQueue에 추가한다.
      만약 사이트 외부의 URL일 경우 ToSendQueue에 추가한다.
    */
    pub fn link_extractor() {
            
            
        }
}
